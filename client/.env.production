# Render backend URL — used by Vite at build time for production builds
# This value is baked into the JS bundle, so it must be set before `npm run build`
# Override this in the Vercel dashboard → Settings → Environment Variables if needed
VITE_API_URL=https://attendance-system-acb5.onrender.com
